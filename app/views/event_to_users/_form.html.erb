<%= form_for(event_to_user) do |f| %>
  <% if event_to_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_to_user.errors.count, "error") %> prohibited this event_to_user from being saved:</h2>

      <ul>
      <% event_to_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %>

    <%= collection_select(:event_to_user, :event_id, Event.all, :id, :name,{:prompt => 'Elige el evento', :class => "form-control"})%>
  </div>


  <div class="field">
    <%= f.label :user_id %>
    <%= collection_select(:telegram_user,:telegram_id,TeleNotify::TelegramUser.all,:id,:first_name,{},{:multiple => true, :class => "form-control", :id =>"exampleSelect2"}) %>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
